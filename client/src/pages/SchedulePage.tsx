import styled from "styled-components";

const SchedulePage = () => {
  return <SchedulePageStyle>일정 추가 페이지</SchedulePageStyle>;
};

const SchedulePageStyle = styled.div``;

export default SchedulePage;
