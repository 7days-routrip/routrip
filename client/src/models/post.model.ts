// 임의로 해논거 이거 나중에 변경 해야해함
export interface Post {
  id: number;
  title: string;
  date: string;
  author: string;
  continental: string;
  country: string;
  postsNum: string;
  postsImg: string;
  like: string;
  commentsNum: string;
  profileImage: string; // 아직 api 에 명시 안됨
}
